const utils = require('../../../../utils/index.js');
const children = ['','so1','so2','so3','so4','so5','so6','so7','so8','so9','so10'];

module.exports = [
    utils.getSidebar('剑指offer',children),
];